High level, JIT compiled, dynamic typed, protoype-based OOP, first-class functions
Multi-paradigm - event-driven, functional, imperative
APIs - text, dates, regex, DOM etci
```
<script>
console.log("Hello, World!");
</script>

<script src="myscript.js"></script>
```

Very loosely typed
```
var x;
let y;
```

DOM
```
document.getElementById("demo").innerHTML = "Hello, World!"
```

```
alert("Hello");
doesConfirm = confirm("Do you confirm?");
```

Control Flow
```
switch(expression) {
	case x:
		break;
	case y:
		break;
	default:
		pass;
}
```